# Audio Analysis (FFT/VQT)

Detailed behavior, parameters, and equations live in `CLAUDE.md` at the repo root.

This page will summarize API-facing behavior (bins, windows, smoothing, normalization, whitening) and cross-link into the canonical derivations in `CLAUDE.md`.

