# Memory Map (Canonical Reference)

Canonical source: `MEMORY_MAP.md` at the repository root. This page exists only to link to it and add short guidance.

Notes
- Tests and specs must reference the root `MEMORY_MAP.md` to avoid drift.
- When documenting APIs that read/write memory (peek/poke/memcpy/memset/vbank/sync), link to the specific sections in `MEMORY_MAP.md`.

